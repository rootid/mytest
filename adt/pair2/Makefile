SHELL+=-x
ROOT=../..
include $(ROOT)/common.mk

EXECS = my-pair

HEADERS = (addsuffix $(EXECS),.h)

OBJS = (addsuffix $(EXECS),.o)

all : $(EXECS)

clean :
	rm -f $(EXECS) *.o *.gch

$(OBJS) : 
	$(CXX) $(CXXFLAGS) -c $<

my-pair.o : my-pair.cpp my-pair.h
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -c $<

main.o :  main.cpp my-pair.cpp
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -c $^

my-pair: main.o my-pair.o
	$(CXX) $(CPPFLAGS) $(CXXFLAGS) -lpthread $^ -o $@

.PHONY: help

help:
	@echo "Please use 'make clean' to remove all files generated by make"
	@echo "Please use \`make <target>' where <target> is one of"
	@echo "  all       to make standalone HTML files"

